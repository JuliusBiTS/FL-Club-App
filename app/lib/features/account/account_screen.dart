import 'package:flutter/material.dart';

import '../../core/widgets/placeholder_screen.dart';

/// The "You" tab — briefing §9.10. Account, tickets, loyalty, membership,
/// notification settings, and the store-mandated delete-account flow.
class AccountScreen extends StatelessWidget {
  const AccountScreen({super.key});

  @override
  Widget build(BuildContext context) => const PlaceholderScreen(title: 'You', milestone: 'M2');
}
